Finalize the Superpowers per-task review fanout.

Verify the latest task review loop approved the implementation and wrote a task
review summary path. Record that path on the source implementation task summary
so the following record-item-result step can commit only after spec compliance
and code quality have both passed.

Close this expansion target with `gc.outcome=pass`.

Do not invoke provider-native subagents or upstream plugin runtime commands.
