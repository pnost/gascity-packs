Coordinate the Superpowers plan-review loop.

The child lanes are Gas City graph steps that replace native subagent dispatch. Wait for the review and fix lanes, then use the design-review approval check to decide whether another loop iteration is needed.

Do not spawn model-native subagents. Re-run or continue only through this
graph stage's child steps.
