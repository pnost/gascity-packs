Implement the minimal Superpowers task change.

Use the approved plan, task bead, and `test-driven-development` guidance. Edit
only the files owned by this task unless the task evidence shows a required
dependency fix. Write the smallest production change that should make the
failing test pass.

Do not perform post-drain review work here. Broader code review, gap analysis,
and review-fix looping run after the convoy drain completes.

Do not invoke provider-native subagents or upstream plugin runtime commands.
