Verify the Superpowers task change.

Run the focused command from the failing-test step and confirm it passes. Then
run the smallest relevant broader verification command from the approved plan or
repository conventions. If either command fails, return to the implementation
change in this same task context until the task evidence is clean.

Record every command, result, and any remaining risk in the task summary. Do
not claim verification from memory or from an unrecorded command.

Do not invoke provider-native subagents or upstream plugin runtime commands.
