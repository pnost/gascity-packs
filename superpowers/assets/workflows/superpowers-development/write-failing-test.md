Write the failing test for this Superpowers implementation task.

Use the task context note, approved plan, and installed
`test-driven-development` guidance. Add or update the smallest test that proves
the next required behavior for this task. Keep the test focused on the task
bead's acceptance criteria and avoid unrelated cleanup.

Record the exact test command that should fail and the expected failure reason.
Close this step only after the test file is written and the command is ready to
run from the task worktree.

Do not invoke provider-native subagents or upstream plugin runtime commands.
