Coordinate the Superpowers written spec loop.

Each attempt follows the stock Superpowers sequence after design approval:
write the spec from the approved design, run the spec self-review, review the
written spec, apply required feedback, then confirm written spec approval. Stop
only when the approval lane records `design_review.verdict=done`.

Do not invoke provider-native subagents. Continue only through this graph
stage's lanes.
